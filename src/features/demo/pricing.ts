import { pricingRules } from "@/features/pricing/pricing-engine";

// Demo pricing mirrors the production pricing engine. When Supabase is connected,
// these values move into pricing_rules and admin-edited settings.
export const demoPricingRules = pricingRules;
